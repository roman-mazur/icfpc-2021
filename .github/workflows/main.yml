name: main
on: [push]
jobs:
  test-and-build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '^1.16'
      - run: go test ./...
      - run: go run ./solutions/submit.go
